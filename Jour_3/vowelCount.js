function vowelCount(str) {

    if (charCodeAt(string.length) == 97 || 101 || 105 || 111 || 117 || 121) {
        return 1;
    }
    else {
        return 0;
    }
}

console.log(vowelCount('hello'))
